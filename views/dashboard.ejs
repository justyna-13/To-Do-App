<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link href="../public/assets/styles_reg.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Poppins:400,500,600" rel="stylesheet">

    <title>Dashboard</title>
</head>

<body>
    <div class="top-bar"></div>
    <div class="blue-grey lighten-5 center-align">
        <div class="container white">
            <div class="date-header">
                <span id="day">Today</span>
                <span id="date">
                    <% var days = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'] %>
                    <% var months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'] %>
                    <% var today = new Date() %>
                    <% var dd = String(today.getDate()).padStart(2, '0') %>
                    <% var mm = months[String(today.getMonth())]; //January is 0! %>
                    <% var weekDay = days[today.getDay()] %>


                    <%= weekDay + ' ' + mm + ' ' + dd %>
                </span>
            </div>
            <div class="list">
                <ul>
                    <% for(item of todoList) { %>
                            <li>
                                <div class="row">
                                    <div class="col s11"><%= item.name %></div>
                                    <form action="/removetask" method="POST">
                                        <input type="hidden" name="removetask" value="<%= item.id %>">
                                        <button class="col s1 remove">x</button>
                                    </form>
                                </div>
                                <div class="divider"></div>
                            </li>
                    <% } %>
                </ul>
            </div>
            <form action="/addtask" method="POST">
                <div class="row">
                    <input class="col s10" id="new-task-title" type="text" name="newtask" placeholder="Add task">
                    <button id="add-task-submit" class="col s2 btn waves-effect waves-light">Add task</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>